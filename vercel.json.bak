{
  "builds": [
    {
      "src": "nuxt.config.js",
      "use": "@nuxt/engine",
      "config": {
        "target": "server"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
